# PostgreSQL — any provider works (Neon, Supabase, Railway, local)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

# Protect cron routes in production
CRON_SECRET=change-me

# Minimum minutes between two auto-scans (prevents duplicate writes)
MIN_UPDATE_INTERVAL_MINUTES=50
